---
// Static-output redirect to /thoughts.
// Firebase will handle the 301 in production via firebase.json (KLY-57).
// In `pnpm preview` and dev, Astro emits a meta-refresh stub HTML.
return Astro.redirect('/thoughts', 301);
---
